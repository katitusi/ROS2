# Демонстрации Igus ReBeL - Инструкция (Русский)

Этот проект содержит три демонстрационных приложения для робота igus ReBeL 6DOF с ROS2 Humble в Docker.

## 📋 Предварительные требования
- Установленный и запущенный **Docker Desktop**
- **Foxglove Studio** (в браузере): [https://studio.foxglove.dev](https://studio.foxglove.dev)
- Минимум 8 ГБ RAM
- Windows 10/11 с PowerShell

## 🚀 Шаг 1: Запуск симуляции

**ВАЖНО**: Перед запуском любой демонстрации необходимо запустить симуляцию!

### Метод 1: Двойной клик (самый простой)
Двойной клик на `start_sim.bat` в папке проекта

### Метод 2: Команда PowerShell
```powershell
Start-Process -FilePath "c:\Users\vladimir.sadikov\OneDrive - BE O365 MS-Cloud\Dokumente\ROS2\start_sim.bat"
```

**Что запускается:**
- Docker контейнер с ROS2 Humble
- Симуляция Igus ReBeL (mock hardware)
- Rosbridge сервер (порт 9090 для Foxglove)
- MoveIt2 планирование движений

**Подождите**, пока не увидите сообщение:
```
✅ Rosbridge ready at ws://localhost:9090
🤖 Starting ReBeL Simulation...
```

---

## 🎯 Шаг 2: Выбор и запуск демонстрации

### 1. **Simple MoveIt Demo** - Базовое управление движением
Простая демонстрация управления роботом через интерфейс MoveIt2.

**Запуск (Метод 1 - Двойной клик):**
Двойной клик на `start_simple_demo.bat`

**Запуск (Метод 2 - PowerShell):**
```powershell
Start-Process -FilePath "c:\Users\vladimir.sadikov\OneDrive - BE O365 MS-Cloud\Dokumente\ROS2\start_simple_demo.bat"
```

**Функции:**
- Интерактивное планирование движений с MoveIt2
- Визуализация в RViz (в контейнере) или Foxglove Studio
- Базовое управление суставами и декартовыми координатами

---

### 2. **Safety Demo** - Мониторинг дистанции до человека
Демонстрация мониторинга близости человека с автоматической аварийной остановкой робота.

**Запуск (Метод 1 - Двойной клик):**
Двойной клик на `start_safety_demo.bat`

**Запуск (Метод 2 - PowerShell):**
```powershell
Start-Process -FilePath "c:\Users\vladimir.sadikov\OneDrive - BE O365 MS-Cloud\Dokumente\ROS2\start_safety_demo.bat"
```

**Компоненты:**
- **Human Distance Publisher**: Симулирует датчик расстояния (публикует в `/human_distance`)
- **ReBeL Mover**: Контроллер MoveIt2 с сервисом включения/отключения
- **LLM Safety Supervisor**: Отслеживает расстояние с настраиваемыми порогами

**Пороги безопасности:**
- `WARN_THRESHOLD`: 1.0м (Предупреждение)
- `DANGER_THRESHOLD`: 0.6м (Остановка робота)

**Тестирование:**
```bash
# В контейнере (новый терминал):
docker exec -it <container-id> bash
source /ws/install/setup.bash

# Опубликовать расстояние (например, 0.5м = опасность):
ros2 topic pub /human_distance std_msgs/msg/Float32 "data: 0.5" --once
```

---

### 3. **Dance Demo** - 12-секундная скоростная хореография
Полная танцевальная хореография с 6 фазами (вдохновлена Boston Dynamics).

**Запуск (Метод 1 - Двойной клик):**
Двойной клик на `start_dance_demo.bat`

**Запуск (Метод 2 - PowerShell):**
```powershell
Start-Process -FilePath "c:\Users\vladimir.sadikov\OneDrive - BE O365 MS-Cloud\Dokumente\ROS2\start_dance_demo.bat"
```

**Фазы хореографии:**
1. **Opening - Greeting** (0-0.8с): Приветственный жест вверх
2. **Wave Motion** (0.8-3.2с): Плавные волнообразные движения
3. **Figure-8 Pattern** (3.2-5.6с): Мягкие круговые узоры
4. **Robot Twist** (5.6-8с): Полный поворот
5. **Grand Finale** (8-11.2с): Быстрые комбинированные движения
6. **Bow** (11.2-12с): Финальный поклон

**Примечание:** Фактическая продолжительность ~47с (75% скорости, в 2.5 раза быстрее).

---

## 🖥️ Визуализация с Foxglove Studio

### Первоначальная настройка:
1. Откройте [https://studio.foxglove.dev](https://studio.foxglove.dev) в браузере
2. Нажмите **Open connection**
3. Выберите **Rosbridge** как тип подключения
4. Адрес: `ws://localhost:9090`
5. **Важно**: Установите Compression в **none**
6. Нажмите **Open**

### Настройка интерфейса:
1. Добавьте панель **3D**
2. В настройках:
   - Display Frame: `world`
   - Topics → Включите Robot Model
3. Опционально: Панель **Plot** для `/human_distance` (Safety Demo)

---

## 🛠️ Ручное управление

### Найти ID контейнера:
```powershell
docker ps
```

### Войти в контейнер:
```powershell
docker exec -it <container-id> bash
source /opt/ros/humble/setup.bash
source /ws/install/setup.bash
```

### Показать доступные топики:
```bash
ros2 topic list
```

### Сервисы Safety Demo:
```bash
# Активировать робота
ros2 service call /rebel_mover/enable std_srvs/srv/SetBool "data: true"

# Деактивировать робота
ros2 service call /rebel_mover/enable std_srvs/srv/SetBool "data: false"
```

### Сервис Dance Demo:
```bash
# Запустить танец (автоматически стартует через 2с)
ros2 service call /rebel_dance_demo/start_dance std_srvs/srv/Trigger
```

---

## 📦 Структура пакетов

```
ROS2/
├── src/
│   ├── rebel_demo/                  # Simple MoveIt Demo
│   │   └── simple_moveit_demo.py
│   ├── rebel_safety_demo/           # Safety Demo
│   │   ├── human_distance_publisher.py
│   │   ├── rebel_mover.py
│   │   └── llm_safety_supervisor.py
│   └── rebel_dance_demo/            # Dance Demo
│       └── rebel_dancer.py
├── start_simple_demo.bat            # Лаунчер Simple Demo
├── start_safety_demo.bat            # Лаунчер Safety Demo
└── start_dance_demo.bat             # Лаунчер Dance Demo
```

---

## 🔧 Устранение неполадок

### Порт 9090 уже занят:
```powershell
# Остановить существующий контейнер
docker ps
docker stop <container-id>
```

### Робот не двигается:
- Проверьте, публикуется ли `/joint_states`:
  ```bash
  ros2 topic echo /joint_states --once
  ```
- Убедитесь, что имена суставов правильные (без подчеркиваний: `joint1`, `joint2`, и т.д.)

### Пересобрать Docker образ:
```powershell
docker-compose build
```

---

## 🚀 Быстрый старт (Краткая инструкция)

### Последовательность действий:
1. **Запустить симуляцию**: Двойной клик на `start_sim.bat` (или команда PowerShell)
2. **Подождать** пока появится "Rosbridge ready"
3. **Запустить демо**: Двойной клик на нужную демонстрацию:
   - `start_simple_demo.bat` - Simple MoveIt Demo
   - `start_safety_demo.bat` - Safety Demo
   - `start_dance_demo.bat` - Dance Demo
4. **Визуализация** (опционально): Браузер → `https://studio.foxglove.dev` → Подключиться к `ws://localhost:9090`
5. **Завершить**: `Ctrl+C` в соответствующем окне batch-файла

### Расположение BAT-файлов:
```
c:\Users\vladimir.sadikov\OneDrive - BE O365 MS-Cloud\Dokumente\ROS2\
├── start_sim.bat              (1. Запустить первым!)
├── start_simple_demo.bat      (2. Потом выбрать демо)
├── start_safety_demo.bat
└── start_dance_demo.bat
```

---

## 📄 Дополнительная документация
- [SAFETY_DEMO_TASK.md](SAFETY_DEMO_TASK.md) - Техническая спецификация Safety Demo
- [DANCE_DEMO_TASK.md](DANCE_DEMO_TASK.md) - Техническая спецификация Dance Demo
- [DOCKER_GUIDE.md](DOCKER_GUIDE.md) - Руководство по Docker
- [IGUS_REBEL_GUIDE.md](IGUS_REBEL_GUIDE.md) - Руководство по оборудованию Igus ReBeL

---

**Удачи с демонстрациями! 🤖**
